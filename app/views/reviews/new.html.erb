<h1>Create Restaurant Review</h1>

<%= form_for @review do |f| %>
  <% if @review.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@review.errors.count, "error") %> prohibited this from being saved:</h2>
    <ul>
    <% @review.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :Title %><br>
    <%= f.text_field :review_title %><br>
  </p>
 
  <p>
    <%= f.label :Review %><br>
	<%= f.text_area :review_body, :cols => 30, :rows => 10 %><br>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>


 <!-- @review.new_record? ? "Create" : "Update" -->